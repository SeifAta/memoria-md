services:

  backend:

    build:
      context: .

    container_name: memoria-backend

    ports:
      - "8000:8000"

    env_file:
      - .env



  frontend:

    build:
      context: ./frontend

    container_name: memoria-frontend

    ports:
      - "3000:80"

    depends_on:
      - backend
